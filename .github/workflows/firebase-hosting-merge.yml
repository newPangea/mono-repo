# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - dev
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]
    env:
      BEFORE_COMMIT: ${{ github.event.before }}
      AFTHER_COMMIT: ${{ github.event.after }}
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: npm cahce
        uses: actions/cache@v2
        id: npm-cache
        with:
          path: ./node_modules
          key: ${{ runner.os }}-npm-cache-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-cache-

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - run: export GOOGLE_APPLICATION_CREDENTIALS="$secrets.FIREBASE_SERVICE_ACCOUNT_NEWPANGEA_DEV"

      - run: echo $GOOGLE_APPLICATION_CREDENTIALS
      # - name: install firebase
      #   run: npm i -g firebase-tools

      # - name: install dependencies
      #   run: npm install
      #   if: steps.npm-cache.outputs.cache-hit != 'true'

      # - name: check affected apps
      #   run: npm run affected:apps -- --base="$BEFORE_COMMIT" --head="$AFTHER_COMMIT"

      # - name: check affected libs
      #   run: npm run affected:libs -- --base="$BEFORE_COMMIT" --head="$AFTHER_COMMIT"

      # - name: Build affected
      #   run: npm run affected:build -- --base="$BEFORE_COMMIT" --head="$AFTHER_COMMIT" --progress=false

      # - uses: FirebaseExtended/action-hosting-deploy@v0
      #   with:
      #     repoToken: '${{ secrets.GITHUB_TOKEN }}'
      #     firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_NEWPANGEA_DEV }}'
      #     channelId: live
      #     projectId: newpangea-dev
      #   env:
      #     FIREBASE_CLI_PREVIEWS: hostingchannels

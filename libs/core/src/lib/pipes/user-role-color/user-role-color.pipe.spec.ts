import { UserRoleColorPipe } from './user-role-color.pipe';

describe('UserRoleColorPipe', () => {
  it('create an instance', () => {
    const pipe = new UserRoleColorPipe();
    expect(pipe).toBeTruthy();
  });
});
